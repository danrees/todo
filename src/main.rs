// use structopt::StructOpt;
// use todo::{FileStore, Store};


/*#[derive(StructOpt,Clone)]
pub enum Commands {
    /// Create a new todo item
    Create{
        #[structopt(short,long)]
        description: String,
    },
}

fn handle(command: Commands, fs: &mut FileStore) {
    match command {
        Commands::Create {description} => {
            fs.new_item(description);
        }
    }
}*/

fn main() {
    /*let mut db = FileStore::new("test.db");
    let matches = Opts::from_args();

    handle(matches,&mut db);

    println!("{:?}", matches);*/
    println!("hello world");
}
